apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: gcp-secretstore
  namespace: frontend
spec:
  provider:
    gcpsm:
      projectID: nemo-v2
      auth:
        secretRef:
          secretAccessKeySecretRef:
            name: gcp-sa-key
            key: nemo-v2-474f3745006a.json