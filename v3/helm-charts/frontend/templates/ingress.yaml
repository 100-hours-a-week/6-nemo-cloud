{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  annotations:
    {{- range $key, $value := .Values.ingress.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host }}
      http:
        paths:
          - path: /api/healthcheck
            pathType: Exact  # 또는 Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80

          # Backend actuator health path (same namespace)
          - path: /actuator/health
            pathType: Exact
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # API paths (same namespace backend)
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # Auth paths (same namespace backend)
          - path: /auth
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # Swagger UI paths (same namespace backend)
          - path: /swagger-ui
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # Swagger resources paths (same namespace backend)  
          - path: /swagger-resources
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080

          # V3 API docs paths (same namespace backend)
          - path: /v3
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080
          # Static images (frontend)
          - path: /images
            pathType: Prefix
            backend:
              service:
                name: {{ $.Release.Name }}
                port:
                  number: {{ $.Values.service.port }}

          # Frontend paths (catch-all, must be last)
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ $.Release.Name }}
                port:
                  number: {{ $.Values.service.port }}
          {{- end }}
    {{- end }}
{{- end }}