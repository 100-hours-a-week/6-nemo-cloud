# ❌ Nginx Ingress Controller 불필요 - AWS Load Balancer Controller 사용 중
# 비용 절약을 위해 제거됨 (Classic LB 비용 $18/월 절약)
#
# 현재 AWS Load Balancer Controller (ALB)로 모든 라우팅 처리 중
# 중복 Ingress Controller 불필요
#
# 필요 시 다시 활성화 가능:
# {{- if .Values.nginxIngress.enabled }}
# apiVersion: argoproj.io/v1alpha1
# kind: Application
# metadata:
#   name: ingress-nginx
#   namespace: argocd
# spec:
#   project: default
#   source:
#     repoURL: https://kubernetes.github.io/ingress-nginx
#     chart: ingress-nginx
#     targetRevision: 4.10.0
#     helm:
#       values: |
#         controller:
#           service:
#             type: LoadBalancer
#   destination:
#     server: https://kubernetes.default.svc
#     namespace: ingress-nginx
#   syncPolicy:
#     automated:
#       prune: true
#       selfHeal: true
# {{- end }}